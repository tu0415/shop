

var filters = {
	toFix: function (value, count) {
		return value.toFixed(count)
	}
}

var strToNumber = {
	toNumber:function(value) {
		return Number(value)
	}
}

module.exports = {
	toFix: filters.toFix,
	toNumber: strToNumber.toNumber
}